/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"name":"Permission","structs":{},"methods":[{"name":"grant","args":[{"type":"address","name":"account"},{"type":"uint64","name":"expiry_timestamp"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"revoke","args":[{"type":"address","name":"account"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"has_permission","args":[{"type":"address","name":"account"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"permission_expiry":{"keyType":"AVMBytes","valueType":"uint64","prefix":"cGVybV8="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[70,103,124],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[79],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"CyADIAEAJgEFcGVybV8xG0EAJDEZFEQxGESCAwRBn3XJBCODFZcEd0cgYTYaAI4DAAkAKgA/ADEZFDEYFBBDNhoBSRUiEkQ2GgJJFYEIEkQxADIJEkQXKE8CUEwWvyNDNhoBSRUiEkQxADIJEkQoTFC8SCNDNhoBSRUiEkQoTFC+TBckTE8CTTIHD4ABACRPAlSABBUffHVMULAjQw==","clear":"C4EBQw=="},"compilerInfo":{"compiler":"puya","compilerVersion":{"major":5,"minor":7,"patch":1}},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Permission contract
 */
export type PermissionArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'grant(address,uint64)void': {
      account: string
      expiryTimestamp: bigint | number
    }
    'revoke(address)void': {
      account: string
    }
    'has_permission(address)bool': {
      account: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'grant(address,uint64)void': [account: string, expiryTimestamp: bigint | number]
    'revoke(address)void': [account: string]
    'has_permission(address)bool': [account: string]
  }
}

/**
 * The return type for each method
 */
export type PermissionReturns = {
  'grant(address,uint64)void': void
  'revoke(address)void': void
  'has_permission(address)bool': boolean
}

/**
 * Defines the types of available calls and state of the Permission smart contract.
 */
export type PermissionTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'grant(address,uint64)void' | 'grant', {
      argsObj: PermissionArgs['obj']['grant(address,uint64)void']
      argsTuple: PermissionArgs['tuple']['grant(address,uint64)void']
      returns: PermissionReturns['grant(address,uint64)void']
    }>
    & Record<'revoke(address)void' | 'revoke', {
      argsObj: PermissionArgs['obj']['revoke(address)void']
      argsTuple: PermissionArgs['tuple']['revoke(address)void']
      returns: PermissionReturns['revoke(address)void']
    }>
    & Record<'has_permission(address)bool' | 'has_permission', {
      argsObj: PermissionArgs['obj']['has_permission(address)bool']
      argsTuple: PermissionArgs['tuple']['has_permission(address)bool']
      returns: PermissionReturns['has_permission(address)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        permissionExpiry: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PermissionSignatures = keyof PermissionTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PermissionNonVoidMethodSignatures = keyof PermissionTypes['methods'] extends infer T ? T extends keyof PermissionTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Permission smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PermissionSignatures> = PermissionTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Permission smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PermissionSignatures> = PermissionTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PermissionTypes['state']['box']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type PermissionCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type PermissionDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: PermissionCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Permission smart contract
 */
export abstract class PermissionParamsFactory {
  /**
   * Constructs a no op call for the grant(address,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static grant(params: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'grant(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account, params.args.expiryTimestamp],
    }
  }
  /**
   * Constructs a no op call for the revoke(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revoke(params: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the has_permission(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hasPermission(params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'has_permission(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the Permission smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class PermissionFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `PermissionFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new PermissionClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new PermissionClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the Permission smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: PermissionDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new PermissionClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Permission smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Permission smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Permission smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new PermissionClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the Permission smart contract
 */
export class PermissionClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PermissionClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Permission app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PermissionClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PermissionNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `PermissionClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PermissionClient> {
    return new PermissionClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PermissionClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PermissionClient> {
    return new PermissionClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Permission smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Permission smart contract using the `grant(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    grant: (params: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PermissionParamsFactory.grant(params))
    },

    /**
     * Makes a call to the Permission smart contract using the `revoke(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revoke: (params: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PermissionParamsFactory.revoke(params))
    },

    /**
     * Makes a call to the Permission smart contract using the `has_permission(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hasPermission: (params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PermissionParamsFactory.hasPermission(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Permission smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Permission smart contract using the `grant(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    grant: (params: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PermissionParamsFactory.grant(params))
    },

    /**
     * Makes a call to the Permission smart contract using the `revoke(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revoke: (params: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PermissionParamsFactory.revoke(params))
    },

    /**
     * Makes a call to the Permission smart contract using the `has_permission(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hasPermission: (params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PermissionParamsFactory.hasPermission(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Permission smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Permission smart contract using the `grant(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    grant: async (params: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PermissionParamsFactory.grant(params))
      return {...result, return: result.return as unknown as (undefined | PermissionReturns['grant(address,uint64)void'])}
    },

    /**
     * Makes a call to the Permission smart contract using the `revoke(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revoke: async (params: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PermissionParamsFactory.revoke(params))
      return {...result, return: result.return as unknown as (undefined | PermissionReturns['revoke(address)void'])}
    },

    /**
     * Makes a call to the Permission smart contract using the `has_permission(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hasPermission: async (params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PermissionParamsFactory.hasPermission(params))
      return {...result, return: result.return as unknown as (undefined | PermissionReturns['has_permission(address)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PermissionClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Permission smart contract using the `has_permission(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hasPermission(params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']>) {
    const result = await this.appClient.send.call(PermissionParamsFactory.hasPermission(params))
    return result.return as unknown as PermissionReturns['has_permission(address)bool']
  }

  /**
   * Methods to access state for the current Permission app
   */
  state = {
    /**
     * Methods to access box state for the current Permission app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the permission_expiry map in box state
       */
      permissionExpiry: {
        /**
         * Get all current values of the permission_expiry map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("permission_expiry")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the permission_expiry map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("permission_expiry", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): PermissionComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a grant(address,uint64)void method call against the Permission contract
       */
      grant(params: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.grant(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revoke(address)void method call against the Permission contract
       */
      revoke(params: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revoke(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a has_permission(address)bool method call against the Permission contract
       */
      hasPermission(params: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hasPermission(params)))
        resultMappers.push((v) => client.decodeReturnValue('has_permission(address)bool', v))
        return this
      },
      /**
       * Add a clear state call to the Permission contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PermissionComposer
  }
}
export type PermissionComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the grant(address,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  grant(params?: CallParams<PermissionArgs['obj']['grant(address,uint64)void'] | PermissionArgs['tuple']['grant(address,uint64)void']>): PermissionComposer<[...TReturns, PermissionReturns['grant(address,uint64)void'] | undefined]>

  /**
   * Calls the revoke(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revoke(params?: CallParams<PermissionArgs['obj']['revoke(address)void'] | PermissionArgs['tuple']['revoke(address)void']>): PermissionComposer<[...TReturns, PermissionReturns['revoke(address)void'] | undefined]>

  /**
   * Calls the has_permission(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hasPermission(params?: CallParams<PermissionArgs['obj']['has_permission(address)bool'] | PermissionArgs['tuple']['has_permission(address)bool']>): PermissionComposer<[...TReturns, PermissionReturns['has_permission(address)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Permission smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PermissionComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PermissionComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PermissionComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PermissionComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PermissionComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PermissionComposerResults<TReturns>>
}
export type PermissionComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

